/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  images: {
    unoptimized: true
  },
  // assetPrefix: "/admin/",
  // basePath: "/admin",
  trailingSlash: true
};

export default nextConfig;
